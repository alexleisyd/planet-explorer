{
  "name": "planet-explorer-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for Planet Explorer. The game itself is the three files in the repo root; www/ is a copy, never an edit target.",
  "scripts": {
    "copy": "node scripts/copy-game.mjs",
    "icons": "node scripts/render-icons.mjs",
    "sync": "npm run copy && cap sync",
    "android": "npm run sync && cap run android",
    "ios": "npm run sync && cap run ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "bundle": "npm run sync && cd android && ./gradlew bundleRelease",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug",
    "archive": "npm run sync && xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Release -destination generic/platform=iOS -archivePath ios/App/build/App.xcarchive archive"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
